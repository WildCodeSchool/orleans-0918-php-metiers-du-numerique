{% extends 'layoutadmin.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
<div class="row">
    {{ form_start(form) }}
    <div class="row">
        <button class="btn myButton">{{ button_label|default('Save') }}</button>
        {{ form_widget(form) }}
    </div>
    {{ form_end(form) }}
</div>

{% endblock %}
