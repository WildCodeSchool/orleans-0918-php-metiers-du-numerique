{% extends 'layoutadmin.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <div class="sectionSpace">
        <div class="container">
            {{ form_start(form) }}
            <div class="row">
                <button class="btn buttonCategory col-md-4 col-sm-12">{{ button_label|default('ajouter une categorie') }}</button>
                <div class="col-md-8 col-sm-12 newCategory">
                    {{ form_widget(form.name) }}
                    {{ form_errors(form.name) }}
                </div>

            </div>
        </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
