{{ form_start(form) }}
<div class="form-group">
    <label class="control-label">{{ form_label(form.notice) }}</label>
    <div class="form-group">
        <div class="input-group mb-3">

            {{ form_widget(form.notice) }}
        </div>
    </div>
</div>

<div class="row justify-content-center">
    <button class="btn btn-primary button-save">{{ button_label|default('Modifier') }}</button>
</div>
{{ form_end(form) }}
