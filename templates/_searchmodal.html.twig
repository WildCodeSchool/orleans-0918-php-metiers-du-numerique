<div id="myModalSearch" class="overlaySearch">
    <a class="closebutton" id="closebtnSearch">&times;</a>
    <div class="container-fluid">
                <div class="row justify-content-center  pt-5 modalSearchMargin">
                    <h1 class="modalTitle">Recherche</h1>
                </div>
            <div class="container">
                {{ form_start(searchForm) }}
                <div class="row justify-content-center pt-5">

                        {{ form_widget(searchForm.search) }}
                        <button type="submit" class="btn-primary btnSubSearch mt-5">Chercher</button>
                    {{ form_end(searchForm) }}
                </div>
            </div>
        </div>
    </div>
</div>