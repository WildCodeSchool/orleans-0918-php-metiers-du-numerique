{{ form_start(form) }}
    {% if learning_center.accepted == 0 %}
        {{ form_widget(form.accepted, {'label' : 'Valider cette organisme de formation', 'attr' : {'class' : 'btn-success'}}) }}
    {% else %}
        {{ form_widget(form.accepted, {'label' : 'Ne plus rendre visible cette organisme de formation', 'attr' : { 'class' : 'btn-warning'}}) }}
    {% endif %}
{{ form_end(form) }}
