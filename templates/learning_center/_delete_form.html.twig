<form method="post" action="{{ path('learning_center_delete', {'id': learning_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ learning_center.id) }}">
    <button class="btn">Delete</button>
</form>
