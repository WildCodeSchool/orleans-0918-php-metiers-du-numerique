<form method="post" action="{{ path('partner_admin_delete', {'id': partner.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce partenaire');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner.id) }}">
    <button class="btnSup"><i class="fas fa-trash-alt"></i></button>
</form>
